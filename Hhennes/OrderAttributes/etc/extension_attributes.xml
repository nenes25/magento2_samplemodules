<?xml version="1.0"?>
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Api/etc/extension_attributes.xsd">
    <!--
        Rajouter les extensions ici ne suffit pas
        Dans le cas de champs de la table il faut les créer via le cfichier db_schema.xml
        Pour tous les champs il faut ajouter un plugin afterGet et afterGetList via les plugins (cf. di.xml )
    -->
    <extension_attributes for="Magento\Sales\Api\Data\OrderInterface">
        <attribute code="sample_boolean_attribute" type="bool"/>
        <attribute code="sample_int_attribute" type="int"/>
        <attribute code="sample_text_attribute" type="string"/>
    </extension_attributes>
</config>
